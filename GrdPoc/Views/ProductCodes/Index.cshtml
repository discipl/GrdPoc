@model IEnumerable<GrdPoc.Models.Entities.ProductCode>

@{
    ViewBag.Title = "Index";
}
<br />
@Html.Partial("../Shared/_CodesTabsView")


<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductCodeCategory.ProductCodeCategoryDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductCodeNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductCodeType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductCodeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCodeCategory.ProductCodeCategoryDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCodeNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCodeType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCodeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductCodeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductCodeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductCodeId })
        </td>
    </tr>
}

</table>
