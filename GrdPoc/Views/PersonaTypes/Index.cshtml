@model IEnumerable<GrdPoc.Models.Entities.PersonaType>

@{
    ViewBag.Title = "Index";
}
<br />
@Html.Partial("../Shared/_AccountsTabsView")


<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PersonaTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PersonaTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PersonaTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PersonaTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PersonaTypeId })
        </td>
    </tr>
}

</table>
